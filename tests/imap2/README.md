# imaptest

## Known issues

Tests are adjusted to pass despite following issues. These should
eventually be resolved.

* No address group syntax support in header parser (fetch-envelope).
* No routed address syntax support in header parser (fetch-envelope).
* MIME preambles are removed (fetch-body-mime).